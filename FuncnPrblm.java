// codechef problem:Functions in java
import java.util.*;
import java.lang.*;
import java.io.*;

/* Name of the class has to be "Main" only if the class is public. */
class FuncnPrblm
{
	public static void main (String[] args) throws java.lang.Exception
	{
	    Scanner scanner = new Scanner(System.in);
	    int A, B, C, D;
        A = scanner.nextInt();
        B = scanner.nextInt();
        C = A * A + 2 * A * B + B * B;
        D = A + B;
        System.out.println(C);
        System.out.println(D);

        A = scanner.nextInt();
        B = scanner.nextInt();
        C = A * A + 2 * A * B + B * B;
        D = A + B;
        System.out.println(C);
        System.out.println(D);

        A = scanner.nextInt();
        B = scanner.nextInt();
        C = A * A + 2 * A * B + B * B;
        D = A + B;
        System.out.println(C);
        System.out.println(D);

        scanner.close();
	}
}
